# 第 4 个月复习 · 框架月(LangGraph + 多 Agent + tracing + MCP)

> **主线**:把手写过的东西(RAG / Agent Loop / 工具 / 记忆)用工业框架 LangGraph 重走一遍
> → 看清"框架到底封装了什么" → 开始摸生产级(限流 / 多 Agent 架构 / 可观测)。
> 对应文件:26~37。

---

## ✍️ 我的核心收获(自己写,别让 Claude 代笔)

- (在这里用自己的话写 3~5 条最戳你的收获)
-
-

---

## 一、Chroma / 向量数据库(26–30)

- **Chroma = 向量数据库**(能装的库):按"相似"查(vs 普通库按"精确"查)。
- 核心价值:**ANN / HNSW 索引**("跳着找",百万级毫秒响应)+ **持久化**(向量不重算)。
- 四个 API:`PersistentClient(path)` / `get_or_create_collection(name, {"hnsw:space":"cosine"})` / `add(ids, embeddings, documents)` / `query(query_embeddings, n_results)`。
- **迁移验证**:手写余弦 vs Chroma 数字全同(hit@3=13/15, MRR=0.734)→ 证明"默认 query = 纯向量余弦";**冻结的评估集 = 重构安全网**。
- Chroma 官方自称"**检索引擎**"(dense / sparse / hybrid / 元数据过滤 / 多模态),但**默认只跑纯向量**;其它是可选、要显式启用的能力。
- **混合检索 + RRF**(倒数排名融合;融合分 = Σ 1/(K+名次);共识 > 单科状元)。
- **亲手实测证伪"hybrid 一定更好"**:小语料 + 强 BGE-m3 下,纯向量已够、关键词反添噪声 → **别信二手结论,量一下再说**。

## 二、ReAct(31)

- **ReAct 是概念**(Thought → Action → Observation → 循环),不是库;手写的 Agent Loop 早就是它。
- 手写 **prompt 版**(不用 function-calling):
  - `stop=["Observation:"]` 掐断生成,防模型自己脑补工具结果;
  - **自己正则解析** `Action: 工具名[参数]`(脆弱的文本解析本体);
  - Observation 由你的代码执行工具后填回。
- 关键悟:**`Observation:` 是 prompt 里自定的约定(源自 ReAct 论文),不是模型内置信号**;`stop` 词、prompt 格式、解析必须三者一致。
- **function-calling 用结构化 `tool_calls` 取代了这套脆弱文本解析** —— 这就是它是进步的原因。

## 三、LangGraph 框架月(32–36)—— 每个文件加一个新概念

| 文件 | 新概念 | = 手写版的 |
|---|---|---|
| 32 | State / Node / Edge / START / END / compile→invoke;`MessagesState` **自动累加消息** | `self.messages` + 手动 `append` |
| 33 | **条件边**(会分岔的边)+ 回环边 | `if msg.tool_calls` + `for turn` |
| 34 | **检索做成一个节点**(RAG) | `retrieve()` + `append(参考资料)` |
| 35 | **tracing**(BaseCallbackHandler 钩子、读 trace) | print 调试 |
| 36 | **supervisor 多 Agent**(子 agent = 工具) | —— |

- **数据靠 state 的 messages 在节点间流**;节点读 state、返回更新,框架自动合并。
- **边的指向**控制"谁重复、谁只跑一次"(retrieve 只在开头,tools↔chat 循环)。
- `llm.invoke(消息列表)` vs `app.invoke({"messages":[...]})`:模型的货币是消息,图的货币是状态。
- **读 trace**:prompt token 每轮涨(历史累加 + 工具 schema 每次重发)= agent 烧钱根源;token 由**服务器 `usage` 字段吐出**。
- **supervisor 靠 tool calling 调度**;**"执行函数 = 调子 agent"**(子 agent 就写在函数体里);**agent 嵌套 agent**。

## 四、⭐ 生产级原理

- **限流**:为什么存在(保护后端 / 公平 / 成本 / 防滥用);**两把锁**:RPM(一段时间总量) vs 并发(同时在飞)。
- **错误分类**:瞬时(429 / 503 / 超时 / 网络)才重试;永久(400 / 401 / 403 / 404)别重试。429="你发太多" vs 503="服务端顶不住"。
- **指数退避 + 抖动**:立刻重试会惊群(thundering herd);等待翻倍 + 掺随机;听 `Retry-After`;设次数/总时上限。
- **幂等**:无副作用(问模型)随便重试;**有副作用(发 Slack / 扣款 / 建单)必须幂等键或隔离**,否则重试 = 重复灾难。→ agent 的"工具执行"要小心。
- **`max_retries=5` 帮你做的**:错误分类 + 退避 + 尊重 Retry-After;**没帮你做的**:RPM 硬额度、你自己工具的副作用幂等。
- **多 Agent 三模式**(判据 = "控制权还给谁 + 要不要共享上下文"):
  - **工具型**(demo):call/return 字符串、控制权不转移、上下文隔离。最简单。
  - **节点型 supervisor**:worker 是图节点、共享 state。
  - **交接型 swarm**:控制权转移 + 共享上下文。
- **并发 × 限流的矛盾**:下游并发受限时,用设计(prompt / 编排)把**并行收成串行**去适配。

## 五、认知升级(比代码值钱)

- **概念 / 库 / 协议** 三分类:RAG、ReAct、Agent = 概念;Chroma、LangGraph = 库;MCP、function-calling = 协议。
- **LangChain(零件箱:模型/工具/消息/回调) vs LangGraph(编排层:状态图)**——一直两个一起用。
- **系统提示词提升"可靠性 / 行为",不给"能力"**:supervisor 不加系统提示词,对"自己会答"的问题会自答、不派活。
- **pydantic**(≈ Swift Codable,所以都有 `model_dump`)、**`@tool` = 装饰器**(≈ SwiftUI `@State`,等价 `f = tool(f)`)。

## 六、零碎但有用

- `from X import a, b`:直接搬进来、用时不带前缀;`.` = 包→子模块路径。
- 字典 `in` 查**键**(O(1) 哈希查表);`not x` 通吃空列表和 None;要区分 None 用 `is None`。
- 对象要 `json.dumps` 先 `model_dump()` 成 dict(序列化边界)。
- 界面和命令冲突时信命令;改文件前先保存/关闭,免得覆盖。

## 七、MCP(37,进行中)

- **MCP = 工具的 USB-C 标准**:把工具从 agent 里拆成独立进程的标准服务,任何 MCP 客户端都能连。
- 进程边界 + 标准协议 + 传输(stdio 本地 / HTTP 远程);`@server.tool()` 暴露工具。
- 待做:写客户端(38)连服务、`list_tools` 发现工具、`call_tool` 跨进程调用。

---

**下一步**:收尾 MCP → Month 5–6 生产实战(生产级 RAG / 向量库 / 多 Agent + FastAPI 服务化 + 评估 + 可观测 + 部署)。
