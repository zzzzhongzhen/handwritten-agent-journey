# 第 2 个月学习清单(手写 Agent Loop)⭐ 转型分水岭

> 目标:不依赖任何框架,亲手写出一个能【自己决定调用工具】完成任务的 agent。
> 做完这个月,你就真正"懂" agent 了,而不是只会调包。
> 节奏:每周 ~9 小时(沿用你的时间表)。

---

## 为什么这个月最重要

90% 的人一上来就学 LangChain,结果只会调框架,框架一变就废、出问题不知道为啥。
你先手写一遍,就彻底理解了 agent 的本质:**一个让 LLM 反复"思考→调工具→看结果→再思考"的循环。**
之后再用任何框架,你都是降维理解。

---

## 核心概念(第 1 周重点啃)

### 1. Tool Calling(工具调用)是 agent 的命脉
LLM 自己不能查天气、不能读文件、不能算数。
但你可以告诉它"你有这些工具",它就会在需要时【输出"我要调用 X 工具,参数是 Y"】。
然后由【你的代码】真正去执行,再把结果喂回给它。

### 2. Agent Loop(智能体循环)
```
while 任务没完成:
    1. 把对话历史 + 可用工具列表,发给模型
    2. 模型回复:要么是最终答案,要么是"我要调用某工具"
    3. 如果是调工具 → 你的代码执行那个工具 → 把结果加进历史 → 回到第 1 步
    4. 如果是最终答案 → 结束,返回答案
```
就这么朴素。所有 agent 框架,底层都是这个循环。

### 3. ReAct 模式
Reason(推理)+ Act(行动)交替。模型先想"我该干啥",再行动,看到结果再想下一步。

---

## 每周排期

### 第 1 周:理解 Tool Calling
- **周一(2h)**:读 Anthropic 文档的 Tool use 章节,理解工具怎么定义(JSON schema)
- **周二(2h)**:跑通 `03_agent_loop.py`,只看"定义一个工具 + 模型决定调用"那部分
- **周三(1h)**:读懂工具的 input_schema 怎么写
- **周四(1h)**:读 Anthropic "Building effective agents" 文章
- **周末(3h)**:🎯 给 agent 加第 2 个工具(比如一个简单计算器)

### 第 2 周:吃透 Agent Loop
- **周一(2h)**:逐行读懂 `03_agent_loop.py` 的 while 循环,画出流程图
- **周二(2h)**:自己默写一遍 agent loop 的骨架,不看原代码
- **周三(1h)**:理解 stop_reason == "tool_use" 的判断逻辑
- **周四(1h)**:复习,把循环讲给自己听(或写成博客)
- **周末(3h)**:🎯 加一个"读本地文件"的工具,让 agent 能回答关于你某个文件的问题

### 第 3 周:多工具 + 健壮性
- **周一(2h)**:让 agent 同时拥有 3 个工具,观察它怎么选
- **周二(2h)**:加错误处理——工具执行失败时,把错误也喂回模型,看它怎么应对
- **周三(1h)**:加防死循环(最大轮数限制)
- **周四(1h)**:读别人写的 mini-agent 源码(GitHub 搜 "minimal agent from scratch")
- **周末(3h)**:🎯 让 agent 完成一个需要多步、多工具配合的任务

### 第 4 周:里程碑 🎯
- **周一(2h)**:设计你自己的 agent——选一个对你有用的小任务
- **周二(2h)**:实现它(复用前几周的工具 + 加你自己的)
- **周三(1h)**:打磨、写注释
- **周四(1h)**:写 README,传 GitHub(第 2 个真实项目)
- **周末(3h)**:🎯 **里程碑**——一个不依赖框架、能调多个工具自主完成任务的 agent

---

## 完成标准
1. 能不看资料、白板手写出 agent loop 的骨架
2. 能解释清楚:tool calling 全流程、为什么要"喂回结果"、stop_reason 的作用
3. GitHub 上有一个你自己的手写 agent 项目

做到这三点 → 你已经超过一大半"只会调 LangChain"的人。进第 3 个月(RAG + 记忆)。
