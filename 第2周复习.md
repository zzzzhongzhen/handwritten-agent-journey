# 第 2 周复习:Python 工程化(异常 / 文件 / JSON / HTTP)

> 本周从"会写语法"进阶到"能写真实工程代码"。这些是调大模型 API 的地基。
> 复习方式:合上文档,看标题能不能口述规则 + 手写出核心代码。

---

## 一、异常处理 try/except(= Swift do/catch)

```python
try:
    可能出错的代码
except ValueError as e:      # 抓具体异常,as e 拿详情(不用可省)
    print(f"出错: {e}")
except Exception as e:       # 兜底:抓所有(只做最后一层)
    ...
else:
    ...                      # 没出错才执行
finally:
    ...                      # 不管怎样都执行(收尾,≈ Swift defer)

raise ValueError("消息")     # 主动抛异常(= Swift throw)
```

**常见异常(第 1 周踩过的,现在能抓了):**
| 异常 | 场景 |
|------|------|
| `IndexError` | 列表下标越界 `nums[10]` |
| `KeyError` | 字典没这个 key `d["x"]` |
| `ValueError` | 值不对 `int("abc")` |
| `TypeError` | 类型不对 `1 + "x"` |
| `ZeroDivisionError` | 除以 0 |

**⚠️ 原则**:能写具体异常就别写 `Exception`。只兜"你预期会出错、且知道怎么处理"的地方;逻辑 bug 应该让它崩,好被发现。

---

## 二、文件读写

```python
with open("file.txt", "r", encoding="utf-8") as f:
    content = f.read()          # 一次读全部 → 一个字符串

# with 用完自动关文件(≈ Swift 自动资源管理),不用手动 close()
```

**三种模式(第二个参数):**
| 模式 | 意思 | 行为 |
|------|------|------|
| `"r"` | 读 | 默认 |
| `"w"` | 写 | **覆盖!原内容清空** |
| `"a"` | 追加 | 接在末尾写 |

**三种读法(结果不同!):**
```python
content = f.read()              # 整个文件 → 一个大字符串
for line in f: ...              # 逐行(流式,省内存,一次性)
lines = f.readlines()          # 所有行 → 一个列表(能索引/切片/重复用,吃内存)

for c in f.read(): ...         # ⚠️ 逐字符!(read 是字符串,遍历字符串=逐字符)
```

**删除某一行的套路**(文件没有"原地删行"):
```python
with open("f.txt") as f:
    lines = f.readlines()
lines = [l for l in lines if "sss" not in l]
lines = [l for l in lines if "要删的内容" not in l]   # 推导式筛掉
with open("f.txt", "w") as f:
    f.writelines(lines)                                # 覆盖写回
```

---

## 三、JSON(⭐ 和 API 对话的通用语言)

```python
import json

json.dumps(对象)      # Python 对象 → JSON 字符串   (带 s = string)
json.loads(字符串)    # JSON 字符串 → Python 对象   (带 s = string)
json.dump(对象, f)    # 写进文件                     (没 s = file)
json.load(f)          # 从文件读                     (没 s = file)
```

**记忆口诀:带 s = 和字符串打交道;不带 s = 和文件打交道。**

**两个常用参数:**
- `ensure_ascii=False` —— 中文正常显示(不加变 `你`)
- `indent=2` —— 美化缩进,好读

**类型对照表(读 API 返回必备):**
| JSON | Python |
|------|--------|
| `{}` object | `dict` |
| `[]` array | `list` |
| `"abc"` | `str` |
| `123` / `1.5` | `int` / `float` |
| `true` / `false` | `True` / `False`(大小写!) |
| `null` | `None` |

---

## 四、HTTP 请求 httpx(= Swift URLSession)

```python
import httpx

response = httpx.get(url, params={"k": "v"}, timeout=10)   # 发 GET
response.status_code       # 200 成功 / 404 找不到 / 500 服务器错
response.text              # 原始字符串
response.json()            # ⭐ 直接解析成 dict(httpx 帮你 json.loads 了)
response.raise_for_status()   # 状态码不是 2xx 就抛异常
```

**生产代码必做:设 timeout + 包 try/except**(网络天生不可靠):
```python
try:
    r = httpx.get(url, timeout=10)
    r.raise_for_status()
    data = r.json()
except httpx.TimeoutException:   ...   # 超时
except httpx.HTTPStatusError:    ...   # 4xx/5xx
except httpx.RequestError:       ...   # 断网/DNS 失败
```

装库:`uv add httpx`

---

## 五、⚠️ 本周特别要注意的坑(重点!)

1. **别用内置名当变量/参数名**(`list`/`dict`/`str`/`set`/`sum`/`id`/`type`)。
   本周又踩一次(参数命名 `list`)。`list[i]` 取值没事,但一旦 `list(...)` 调用就崩。**起名多想一秒。**

2. **`json.dump` vs `dumps` 别混**。`dump`(文件版)返回 `None`,别去接返回值;`dumps` 才返回字符串。里程碑里踩过。

3. **`with`/`if`/`for` 不创建作用域**。里面赋值的变量出来照样能用(只有函数才隔离)。`content` 出了 `with` 能读到,就是这个原因。

4. **`for x in f.read()` 是逐字符,不是逐行**。想逐行用 `for line in f`。

5. **`"w"` 模式会清空原文件**。别拿它开你不想丢内容的文件,追加用 `"a"`。

6. **网络请求一定设 `timeout` + 包 `try/except`**。不设 timeout 可能卡死。

7. **中文记得 `ensure_ascii=False`**,不然 JSON 里中文变 `\uxxxx`。

8. **精确抓异常,别一律 `except Exception`**。兜底只做最后一层。

---

## 自测:合上文档,你能答吗?

- [ ] `json.dumps` 和 `json.dump` 区别?带 s 是干嘛的?
- [ ] `"r"` `"w"` `"a"` 三种模式分别干嘛?哪个会覆盖?
- [ ] `for line in f` 和 `f.readlines()` 有啥不同?大文件用哪个?
- [ ] `for x in f.read()` 遍历出来的是行还是字符?为什么?
- [ ] `with` 会不会创建新作用域?`content` 为什么出了 with 还能用?
- [ ] `response.json()` 帮你做了什么?
- [ ] `raise_for_status()` 什么时候抛异常?
- [ ] JSON 的 `true`/`null` 对应 Python 的什么?
- [ ] 为什么网络请求一定要包 try/except?

能全部口述 + 手写出核心代码 = 第 2 周真正通关。
```

---

## 下周预告:第 3 周打通 LLM API
天气脚本已把"发请求 → 收 JSON → 解析"管道打通。调大模型只是:换成 Anthropic 的网址 + 带上 API key。你已经把最难的地基打好了。
