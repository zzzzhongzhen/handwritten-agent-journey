# 第 1 周复习:Python 基础全景(给 iOS 老兵)

> 本周学的所有东西一次过一遍。对照 Swift 看差异,重点在"和 Swift 不一样的地方"。
> 学习方式:合上文档,看每节标题能不能口述规则;能说出来才算真会。

---

## 一、心智差异(最根本的 5 条)

| 方面 | Swift | Python |
|------|-------|--------|
| 代码块 | `{ }` 花括号 | **缩进**(4 空格,缩进必须有理由——上一行以 `:` 结尾) |
| 类型 | 静态,编译期检查 | 动态,运行期才知道;**类型注解不强制**(纯给人/IDE 看) |
| 空值 | `nil` + Optional | `None`(判断用 `is None`,不用 `==`) |
| 命名 | camelCase | snake_case |
| 长度 | `arr.count` | `len(arr)` |

---

## 二、变量 / 字符串 / 列表

```python
name = "byron"                 # 没有 let/var
print(f"我是 {name}")           # f-string 插值 = Swift 的 \(name)

nums = [1, 2, 3, 4, 5]
nums[-1]          # 5,负索引取倒数,Swift 没有
nums[1:3]         # 切片 [2, 3],取索引 1~2(不含 3)
nums[:10]         # 前 10 个(不够就全给,不崩)
len(nums)         # 长度,不是 nums.count
```

**坑**:变量名别用内置名(`list`/`dict`/`str`/`set`/`sum`/`id`...),会把工具覆盖掉,后面调用就崩。

---

## 三、推导式(Python 特色,Swift 用 map/filter)

```python
doubled = [x * 2 for x in nums]                # map
evens   = [x for x in nums if x % 2 == 0]      # filter
pairs   = [(x, y) for x in a for y in b if x != y]   # 双层循环
squares = {x: x**2 for x in (2, 4, 6)}         # 字典推导式
uniq    = {x for x in "abracadabra" if x not in "abc"}  # 集合推导式
```

---

## 四、四种容器

```python
# list 列表:有序、可变、可重复
arr = [1, 2, 3];  arr.append(4);  arr.pop(1);  arr.count(2);  arr.index(3)

# dict 字典:键值对
d = {"a": 1}
d.get("b", 0)          # 取不到给默认值 0,不崩(vs d["b"] 会 KeyError)
d["a"] = d.get("a", 0) + 1   # 读改写模板 = Swift d["a", default: 0] += 1
for k, v in d.items(): ...   # 遍历键值对

# set 集合:无序、去重
s = {1, 2, 2, 3}       # → {1, 2, 3}
a - b   # 差集(在 a 不在 b)
a | b   # 并集
a & b   # 交集
a ^ b   # 对称差(只在一边)

# tuple 元组:不可变
t = 1, 2, "x"          # 不用括号也行
t[0]
```

**坑**:遍历字典时不能同时删它 → 遍历副本 `for k in d.copy(): del d[k]`。

---

## 五、函数(第 4 章重点)

```python
# 基本 + 默认值 + 类型注解
def greet(name: str, times: int = 1) -> str:
    return f"你好 {name}" * times

# 关键字参数:调用时指名,不用记顺序
make_user(name="byron", city="上海", job="Agent")

# 返回多个值(其实是返回元组)
def min_max(nums): return min(nums), max(nums)
low, high = min_max([3, 1, 9])

# *args / **kwargs:收集多余参数
def f(*args, **kwargs):    # args→元组, kwargs→字典
    ...
f(1, 2, key="v")           # args=(1,2), kwargs={"key":"v"}

# 调用时 * / ** 反过来是"拆开"
info = {"name": "byron"}
greet(**info)              # 等于 greet(name="byron")

# / 和 * 特殊标记(文档用,不是你打的)
def h(pos_only, /, normal, *, kw_only):
    ...
# / 左边只能按位置传;* 右边只能按关键字传

# lambda:临时小函数 = Swift 闭包
add = lambda x, y: x + y
sorted(data, key=lambda p: p[1], reverse=True)   # key 必须关键字传!

# 文档字符串:三引号,存在 __doc__,help() 能读
def foo():
    """这是说明。以后 Agent 工具的描述就靠它。"""
```

**坑**:
- `sorted(...)` 返回**新列表**要接住;`list.sort()` 是原地改不返回。
- 别写 `def f(x=[])`(可变默认参数坑),用 `def f(x=None)`。

---

## 六、控制流

```python
# if / elif / else —— 冒号 + 缩进,没有花括号
if x > 0: ...
elif x == 0: ...
else: ...

# for —— 直接遍历元素
for item in arr: ...
for i in range(5): ...          # 0~4

# while + break/continue,for/while 还能带 else(循环正常结束才执行)
for x in nums:
    if 条件: break
else:
    print("没 break,正常结束")

# match —— 对照 Swift switch
match command.split():
    case ["go", direction]: ...       # 结构匹配 + 捕获
    case [1, 2, 3]: ...
    case _: ...                        # 默认(通配)
# 坑:case 里裸变量名是"捕获"不是"比较"

# pass —— 空占位(Python 没花括号,用它表示"这里空的")
class MyClass:
    pass
```

---

## 七、深入条件(5.7,最反直觉)

```python
# in / not in —— 判断在不在里面
"ell" in "hello"          # True,子串也行
3 in [1, 2, 3]            # True

# is / is not —— 比是不是同一个对象(判 None 用它)
x is None                 # ✅
[1,2] == [1,2]            # True,值相等
[1,2] is [1,2]            # False,不是同一个对象

# 比较可串联(Swift 不行)
1 < x < 10                # 等于 x>1 且 x<10

# and / or 返回操作数本身,不是 Bool!(短路)
"" or 0 or "last"         # 'last'(or 找第一个真的)
"a" and "" and "b"        # ''(and 找第一个假的)
name = user_input or "匿名"   # 空值兜底常用法

# 优先级:and 比 or 先算(拿不准就加括号)
"" or "a" and "b"         # 'b'
```

**假值表**(这些当"假"):`False` `0` `0.0` `""` `[]` `{}` `()` `None`。其余全真。
所以可直接写 `if my_list:`(非空为真)、`if not d:`(空为真)。
⚠️ `"0"`(带引号)是**非空字符串 → 真**;只有数字 `0` 是假。

---

## 八、循环技巧(5.6)+ 序列比较(5.8)

```python
# enumerate:同时拿下标 + 元素(代替 range(len(...)))
for i, item in enumerate(arr): ...

# zip:并排遍历多个列表(对齐到最短!多的被丢)
for q, a in zip(questions, answers): ...

# 序列比大小:逐元素字典序,前缀短的算小
[1,2,3] < [1,2,4]         # True
"abc" < "abd"             # True(字符串逐字符比)
1 < "2"                   # ❌ TypeError,不同类型不能比
```

**坑**:`reversed()`/`enumerate()`/`zip()`/`map()` 返回的是**迭代器**,`print` 出来是 `<...object>`,要 `list(...)` 展开才看到内容。

---

## 九、async(混个眼熟,你 iOS 用过)

```python
import asyncio
async def slow():
    await asyncio.sleep(1)
    return "done"
asyncio.run(main())        # 顶层入口
```

---

## 本周"踩坑"清单(啃过才会的经验)

1. 缩进要有理由(上一行 `:` 结尾才该缩进)
2. 变量名覆盖内置 `list` → 后面 `list(...)` 崩
3. `.copy` 忘了 `()` → 拿到的是方法对象不是结果
4. 类型注解不强制,写错类型运行时不报错
5. `sorted` 的 key 必须 `key=` 关键字传;返回值要接住
6. 迭代器要 `list()` 展开才能看内容
7. for 循环变量循环结束后会泄漏到外面
8. `and`/`or` 返回操作数本身,不是 True/False

---

## 自测:合上这份文档,你能口述吗?

- [ ] `len(x)` 和 `x.count` 谁对?负索引 `-1` 取谁?
- [ ] `dict.get(k, 0)` 干嘛的?为什么不直接 `dict[k]`?
- [ ] `*args` 和 `**kwargs` 分别收集什么、打包成什么类型?
- [ ] 判断 None 用 `is` 还是 `==`?为什么?
- [ ] `"" or "hi"` 输出什么?`"a" and ""` 呢?
- [ ] `enumerate` 和 `zip` 各干嘛?zip 长度不齐会怎样?
- [ ] `sorted()` 和 `list.sort()` 区别?
- [ ] `set` 的 `- | & ^` 分别是什么运算?

能全部口述 = 第 1 周真正通关。说不出来的,回上面对应章节再看一眼。
