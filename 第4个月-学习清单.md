# 第 4 个月学习清单(框架 + MCP + 多 Agent + tracing)

> 目标:用 LangGraph 重写你手写过的 agent,亲眼看清"框架到底封装了什么";
> 学会 MCP;做出第一个 supervisor 多 Agent;开着 tracing 跑通一条完整调用链。
> 节奏:每周 ~9 小时(周一二晚 2h 硬活、周三四晚 1h 轻活、周末 3h 里程碑)。

---

## 这个月的正确心态

原计划说"框架月最不重要"——对一半。**框架本身**确实不重要(你手写过,学框架是降维理解);
但这个月挂了两个生产层补丁:**supervisor 多 Agent** 和 **tracing**,这两个是 2026 年 JD 里的硬通货。
所以这个月的重点不是"学会 LangGraph",而是:

1. 每写一段框架代码,都回答一个问题:**"这行为我手写版里的哪几行?"**(对照 14/19/21 号)
2. 借框架第一次摸到手写不划算的东西:图结构编排、多 Agent、可视化 trace

**铁律不变:代码全部你亲手写。清单只给任务和 API 名,不给成品。**

---

## 核心概念(第 1 周啃)

### 1. LangGraph 的世界观:Agent = 状态图
你手写的 agent 是 `while` 循环;LangGraph 把它显式化成**图**:
节点(node)= 一步操作(调模型/执行工具),边(edge)= 下一步去哪,状态(state)= 你的 messages。
你的 `for turn in range(max_turns)` + `if tool_calls` 分支,在图里就是"条件边"。

### 2. 为什么图比循环值钱
循环只能直线走;图能表达分支、并行、多 Agent 协作、human-in-the-loop 暂停。
这是 JD 里"agent 编排(orchestration)"这个词的本义。

### 3. Supervisor 模式(第 3 周做)
一个"调度 agent"看任务,决定分派给哪个"子 agent"(每个子 agent 有自己的工具和 prompt),
子 agent 干完汇报,调度者决定继续派活还是收尾。——多 Agent 系统最常见的形态。

### 4. MCP(第 4 周做)
Model Context Protocol:把"工具"从 agent 代码里拆出去,变成独立的 server,
任何 agent(Claude、你写的、别人写的)都能连上来用。类比:工具的 USB 标准。
你手写版里工具和 agent 焊死在一个文件里;MCP 就是把它们解耦成客户端/服务端。

---

## 每周排期

### 第 1 周:LangGraph 重写 Agent Loop(对照手写版)
- **周一(2h)**:装环境(`uv add langgraph langchain-openai`);读 LangGraph 快速入门;
  跑通最小图:一个只会聊天的 graph(ChatOpenAI 接 Kimi:base_url 老配方)
- **周二(2h)**:🔨 `30_langgraph_loop.py`——用 LangGraph 重写 14 号(calculate + get_weather 两个工具),
  用预制的 ReAct agent(`create_react_agent`)先跑通
- **周三(1h)**:读文档:StateGraph、节点、边、条件边的概念
- **周四(1h)**:对照作业(写进 note 或 README):列一张表——手写版的每个部件(循环/分发/塞结果/max_turns)
  对应 LangGraph 的什么
- **周末(3h)**:🎯 不用预制件,自己用 StateGraph 手搭一遍同样的 agent(节点+条件边),
  和 create_react_agent 版对照——这一步做完,"框架封装了什么"就不是玄学了

### 第 2 周:RAG 迁移 + tracing 开箱
- **周一(2h)**:🔨 `31_langgraph_rag.py`——把 21/27 号的向量 RAG agent 迁到 LangGraph
  (检索做成一个节点;Chroma 和硅基流动 embedding 原样复用)
- **周二(2h)**:接 tracing:优先试 LangSmith 免费版(注册若被墙/受限,换开源 Langfuse,自托管或云免费层);
  跑几条对话,亲眼看一条 trace 里每次模型调用/工具调用/token 数
- **周三(1h)**:轻活:在 trace 界面里回答三个问题——哪一步最耗 token?哪一步最慢?工具参数长什么样?
- **周四(1h)**:读文档:LangGraph 的 checkpointer(它的"持久记忆",对照你 22 号的 json 存档)
- **周末(3h)**:🎯 给 31 号加 checkpointer 持久化,验证跨会话记忆;和 22 号手写版对比优劣

### 第 3 周:supervisor 多 Agent ⭐(生产层补丁)
- **周一(2h)**:读 LangGraph 多 Agent 文档(supervisor 模式);设计你的三角:
  建议:调度者 + "查资料 agent"(带 RAG 工具)+ "算数/天气 agent"(带 calculate/get_weather)
- **周二(2h)**:🔨 `32_supervisor.py`——先搭两个子 agent,单独测通
- **周三(1h)**:轻活:读懂 supervisor 的路由原理(它怎么决定派给谁——本质还是 tool calling!
  子 agent 在调度者眼里就是一个"工具")
- **周四(1h)**:复习 + 想两个能考出"派活正确性"的测试问题
- **周末(3h)**:🎯 **里程碑**——supervisor 跑通:一句话任务("查一下请假流程,再算 3 天是多少小时")
  被正确拆派给两个子 agent 并汇总。开着 tracing 跑,截一张多 Agent trace 图(简历素材)

### 第 4 周:MCP + 月度封箱 🎯
- **周一(2h)**:读 modelcontextprotocol.io 概念;装官方 Python SDK(`uv add "mcp[cli]"`)
- **周二(2h)**:🔨 `33_mcp_server.py`——把你的 get_weather + calculate 包成 MCP server,
  用 MCP Inspector(或 Claude Code 连接)验证工具能被发现和调用
- **周三(1h)**:轻活:理解 stdio vs HTTP 两种 transport;想清楚"MCP server 和我手写的 TOOL_FUNCTIONS
  字典有什么本质区别"(答案:进程边界 + 标准协议 = 任何客户端可用)
- **周四(1h)**:复习整月;更新 README 演进地图(30-33 号 + 阶段五)
- **周末(3h)**:🎯 **月度里程碑**——让你的 LangGraph agent 通过 MCP 客户端调用自己的 MCP server 工具
  (工具彻底解耦);git 提交封箱

---

## 完成标准
1. 能讲清楚:LangGraph 的 graph/state/条件边各对应手写版的什么;框架封装了哪些脏活(流式拼装/重试/持久化)
2. 有一个能跑的 supervisor 多 Agent,并能解释"子 agent 对调度者来说就是工具"
3. 有一个自己写的 MCP server,懂 stdio/HTTP transport 的区别
4. 看过自己 agent 的 trace,能指出最耗 token 的环节
5. GitHub 上多 4 个文件(30-33)+ README 更新

做到这五点 → 进第 5-6 月作品期(ClickUp 巡检 agent 生产化,带五件套)。

---

## 提醒
- 遇到框架报错先看 trace/源码,别急着搜——你手写过底层,大概率能猜到它在干嘛(这是你的优势)
- LangSmith/Langfuse 的 key 进 `.env`,别进代码(老规矩)
- 卡住超 30 分钟记下来,周末或问 Claude
