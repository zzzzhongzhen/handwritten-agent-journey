# 大模型原理 Tier1 · 面试速记

> 工作级理解:够答 JD 里"懂大模型原理",够接后面的微调。
> 配套手写实验:`41_attention_by_hand.py`(手算 attention)、`42_next_token.py`(logits→温度→采样)。
> 一句话锚:**通篇就一个动作——点积算相似度**(你手写过 cosine 检索,早会了)。

---

## Part 1 · 模型怎么算(前向:一次吐一个词)

**主线一句话**:
文本 → 拆 token → 每 token 变向量 → attention 吸收上下文 → 取最后一个词的向量 → LM head 投影成词表分数 logits → softmax+采样 → 吐下一个词 → 接回去重复(自回归)。

**五步流水线**

| 步 | 干什么 | 讲法 |
|---|---|---|
| ① 分词 | 文本切 token,查表变向量 | 模型不认字,先切 token,每个查 embedding 表变向量 |
| ② Attention | 每个词吸收上下文 | 每词跟所有词点积算相似度→softmax 成权重→加权融合大家的 V;孤立向量进、带上下文向量出 |
| ③ 叠层 | attention+FFN 叠几十层,多头 | 堆几十层,每层 multi-head 从不同角度看(语法/指代/…) |
| ④ LM head | 最后一个词的向量→词表分数 | 自回归:只用最后位置向量,乘大矩阵 W_lm[d,词表]→logits |
| ⑤ 采样 | logits→选词 | ÷温度 → softmax → top-p 砍长尾 → 抽一个 = 下一个 token |

**Q/K/V**:Q=我在找什么(征友要求),K=我挂的招牌,V=我真实的家当。三步=Q·K 打分→softmax 归一→加权求和 V。
- K/V 分开:招牌(被搜到用)≠ 内容(取走的),像图书馆书名 vs 书里内容。
- 真模型里 Q=X·W_q / K=X·W_k / V=X·W_v,三个矩阵**训练学出来**。**"模型学会关注什么"物理上=这几个矩阵被调成什么样**(43 号手工填个"代词→找名词"的 W_q,"它"的注意力就从自恋跳到两个名词)。
- 真公式多两处:`softmax(Q·Kᵀ/√d_k)·V`(÷√d_k 防点积过大);GPT 加**因果掩码**(只能看前面的词,呼应自回归)。
- **Multi-head**:并排多套 Q/K/V,各抓一种关系(指代/主谓/修饰);**叠几十层**:信息传更远、更抽象。

**采样三旋钮**
- **温度 T**:softmax 前 logits÷T。T 小→分布尖→确定(T=0=贪心,可复现);T 大→分布平→随机有创意。不改排序,只改差距悬殊程度。
- **top-k**:留分数前 k 个,其余归零(k 固定,较老)。
- **top-p(核采样)**:从高到低累加概率到 p 为止,后面砍掉(个数动态,更聪明,主流)。
- 顺序:`logits → ÷T → 砍长尾(top-p) → 归一化 → 抽`
- 默认:要稳/可复现 `T=0`;要创意不失控 `T=0.9, top_p=0.9`。

**点睛记忆锚**
1. attention 管"读懂",LM head 管"吐字"——两个不同零件。
2. 通篇一个动作:点积算相似度(attention 算词间;LM head 算向量像哪个词)。
3. 只用最后一个词的向量预测下一个(自回归本质)。

---

## Part 2 · 参数怎么来(训练:那些矩阵里的数字)

**总框架**:训练 = 调那几十亿个数字,让预测越来越对(预测错→梯度下降挪一点→挪几万亿次)。

| 阶段 | 任务 | 数据 | 学到什么 | 代价 |
|---|---|---|---|---|
| ① 预训练 | 预测下一个词 | 几十 TB 全网文本(无需标注) | **99% 的知识和能力** | 几千 GPU×几月,百万美元级,只有大厂 |
| ② SFT 指令微调 | **还是预测下一个词** | 几万~几十万条 (指令,好答案) 对 | **行为和格式**(学会当助手),不加新知识 | 便宜,几 GPU×几小时~几天 |
| ③ 对齐 RLHF/DPO | 偏好排序 | 人对多个答案排序(A 比 B 好) | **品味/语气/边界**(有用无害诚实、该不该拒绝) | 中等 |

- **①②损失函数相同**(预测下一个词),只是数据从全网换成精选问答对;③换成偏好。
- **base vs instruct**:base=只过①(只会续写不会对话);Instruct/Chat=过了②③(你本地 Qwen2.5-7B-**Instruct** 就是)。
- **知识几乎全在①,②③只改行为不加知识** → 想给模型新事实用 **RAG**,不是微调(两者互补)。
- **你的定位**:①做不起也不用;你干**②(LoRA/QLoRA 微调)**,可能碰一点③(DPO)。

**RLHF vs DPO**:RLHF=先训打分模型再用 PPO 强化学习(复杂不稳);DPO=直接用好/坏答案对调(简单稳定,现主流)。

---

## Part 3 · 前沿补丁(经典三阶段之外,2024 后的大变化)

- **推理模型 / RL 训练(o1、DeepSeek-R1 类)**:在 SFT/对齐后,用强化学习专训"先想一长串再答"(long CoT)。奖励靠**答案对不对**自动判(数学/代码可验证),叫 **RLVR**;DeepSeek-R1 用的 **GRPO** 属这条线。
- 这是最近**最大范式变化**,面试提一句会加分。
- ⚠️ 我(Claude)知识截止 2026-01,前沿细节可能过时——前沿别只信一个来源。

---

## 高频面试题速答

- **Transformer 核心?** Self-attention:每词点积算相关度、加权融合;比 RNN 能并行、看长依赖。
- **怎么生成文字?** 自回归一次一 token:最后位置向量→词表概率→采样→接回→重复。
- **温度?** softmax 前 ÷T,小则确定大则随机,T=0=贪心可复现。
- **top-p/top-k?** 采样前砍长尾,防抽到垃圾词;top-p 个数动态更主流。
- **预训练 vs 微调?** 预训练学知识(贵、全网、预测下一词);微调学行为(便宜、问答对、同样损失)。
- **SFT vs RLHF?** SFT 教"会回答"(监督问答对);RLHF/DPO 教"答得好"(人类偏好排序)。
- **微调能给模型新知识吗?** 基本不能,知识在预训练;新知识用 RAG。
