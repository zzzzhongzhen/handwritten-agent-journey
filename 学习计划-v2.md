# 学习计划 v2 —— Agent 开发 + 端侧 AI 差异化(2026-08 修订)

> **变化**:在原 6 个月计划基础上,整合进 4 块新内容——**本地部署、大模型原理、微调、端侧 AI**。
> **总时长:6 个月 → 约 7.5–8 个月(净增 ~1.5–2 个月)**。
> **主线不变**:先把 Agent 开发学扎实;端侧/微调作为**后置的差异化 phase**,不打乱现有节奏。
> 定位策略:发挥 10 年 iOS 优势,做 **"Agent × 端侧 iOS"** 这个稀缺组合。

---

## 🧭 总览(时间线)

| 阶段 | 内容 | 周期 | 状态 |
|---|---|---|---|
| M1 | Python + LLM API | 1 月 | ✅ 完成 |
| M2 | 手写 Agent Loop + 工具调用 | 1 月 | ✅ 完成 |
| M3 | RAG + 记忆(向量检索 / 评估 / Chroma) | 1 月 | ✅ 完成 |
| **M4** | 框架(LangGraph)+ MCP **➕ 本地部署(Ollama)** | 1 月 +1 周 | 🔵 进行中 |
| 穿插 | **大模型原理 Tier 1**(不占独立周) | 半周 | 🆕 |
| **M5–6** | 主力作品:ClickUp 巡检 agent 生产化 | 2 月 | ⏳ 原计划 |
| **M7–8** | **端侧 + 微调 差异化 phase** | ~1.5–2 月 | 🆕 |

新增净时间 ≈ **6–8 周**(本地部署 ~1周 + 微调 ~2-3周 + 端侧 ~3-4周;原理穿插不计)。

---

## 🔵 M4:框架月 + 本地部署(当前)

- ✅ 32 最小图 / 33 工具+条件边 / 34 RAG 节点 / 35 tracing / 36 supervisor
- ⏳ 37 MCP server + **38 MCP 客户端**(收尾)
- 🆕 **本地部署(插入,~1周)**:
  - 装 Ollama,在 Mac 上跑起一个开源模型(Qwen / Llama 小模型)
  - 理解:量化、GGUF、上下文、为什么本地模型更小更弱
  - **把 supervisor / agent 从 Kimi 换成本地模型** → 摆脱 RPM=3 限流,免费离线
  - 收获:①解当前限流痛点 ②给端侧开头 ③原理在实践里学(量化/推理)

## 🟢 穿插:大模型原理 Tier 1(半周,不设独立周)

对照 JD "懂一点大模型原理",补齐工作级理解(**不碰数学/训练**):
- 注意力/Transformer 直觉、训练三阶段(预训练→SFT→RLHF)
- token/上下文/采样/幻觉/量化/in-context learning
- ⭐ **RAG vs 微调 vs prompt** 的取舍(面试高频)
- 缺口:主要是"注意力直觉 + 训练三阶段",其余大半已会

## ⏳ M5–6:主力作品 —— ClickUp 巡检 agent 生产化(原计划不变)

用真实系统(真 bug 单 / 真 Slack / 真定时任务)做一个**能上线**的 agent,带五件套:
1. 评估集(检索/回答质量,含 Recall@k 多目标)
2. 成本统计(token / 缓存)
3. 安全护栏(prompt injection / 输出校验)
4. 可观测(tracing / 结构化日志)
5. FastAPI 服务化 + 部署
- 生产级 RAG 深化在这里落地:hybrid 调参、reranking、查询分解、迭代/Agentic 检索(先有真问题再上)

## 🆕 M7–8:端侧 + 微调 差异化 phase(压轴)

**这是最锋利的差异化钉子:微调一个小模型 → 量化 → 跑在 iOS 端侧。**

- **第 1 步:微调(~2–3 周)**
  - 概念 + 决策框架(何时微调 vs RAG)
  - 动手 LoRA/QLoRA:数据准备 → 训练小模型 → 评估前后对比(用你会的评估集方法)
  - 可在 Mac 上用 MLX 做
- **第 2 步:端侧部署(~3–4 周)**
  - Apple 端侧栈:Core ML / MLX / **Foundation Models 框架(iOS 26,Swift 调端侧 LLM,自带 tool calling + @Generable 结构化生成)**
  - 模型转换 / 量化,跑在 iPhone / Mac
  - **1–2 个 on-device 项目**:如"纯端侧、离线、隐私优先的 iOS Agent"(本地 RAG + 端侧模型)
  - 你学的 Agent 概念(RAG/工具/评估)全部迁移,只是换本地模型
- **产出**:一个别人做不出的作品——"我既懂 Agent,又能把它跑在端侧 iOS"

---

## 📌 优先级(别一次全塞)

1. **Agent 主线**(M4 收尾 + M5-6 作品)—— 最重要,先扎实
2. **本地部署**(近期,M4 尾)—— 实用 + 解限流 + 接端侧,性价比最高,早玩
3. **原理 Tier 1**(想补时半周)—— 面试前必过
4. **微调 + 端侧**(M7-8 合并成差异化作品)—— 压轴,那时功底扎实、事半功倍

## ⚠️ 铁律(不变)

- **代码全部我亲手写**:Claude 搭骨架 + 留 `____`/TODO,核心我填。
- **要生产级实战 + 讲透底层原理**:不接受"调 API 跑通就完事"。
- **仓库只推个人号 zzzzhongzhen**(SSH 别名 github-personal),绝不用工作号。
- **别断线**:按真实进度走、不死磕日历;已超前约 2 个月,断了主动回来。
