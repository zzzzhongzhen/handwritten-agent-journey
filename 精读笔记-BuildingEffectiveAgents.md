# 《Building effective agents》精读笔记

> 原文:Anthropic,2024-12。业内公认的 Agent 架构"清醒剂"。
> 本笔记按原文结构全量整理,并标注与本仓库代码的对应关系。

## 0. 文章的出发点

Anthropic 和几十个团队合作构建 agent 后发现:**最成功的实现用的不是复杂框架,
而是简单、可组合的模式。** 全文就是把这些模式摊开讲。

## 1. 定义

- **Agentic systems**:总称
- **Workflow**:LLM 和工具按**预定义代码路径**编排——控制流在代码手里
- **Agent**:LLM **动态决定自己的流程和工具使用**——控制流在模型手里

判据一句话:**方向盘在谁手上。**

## 2. 什么时候用 / 不用 agent

- 永远先找最简单的方案,只在确有必要时增加复杂度
- Agentic 系统的本质交易:**用延迟和成本换任务表现**
- 任务清晰、要可预测 → workflow;需要灵活自主 → agent
- 很多应用一次 LLM 调用 + 检索 + few-shot 就够了,根本不需要 agent

## 3. 关于框架的警告

- 框架(LangGraph 等)简化底层,但**多了抽象层,遮住真实 prompt/响应,调试变难**;
  还会诱惑你堆不必要的复杂度
- **建议从直接调 API 开始**——多数模式几行代码就能写;用框架必须懂它底层
- (这正是本仓库"先手写后框架"路线的官方依据)

## 4. 基础积木:Augmented LLM

一切的基本单元 = **LLM + 检索 + 工具 + 记忆**(→ 本仓库 22 号就是这个积木)。
- 模型能主动使用这些能力(自己生成检索词、选工具、决定记什么)
- 接口要好用、文档要清楚;可用 MCP 低成本接入第三方工具生态

## 5. 五种 Workflow 模式

| 模式 | 是什么 | 何时用 | 原文例子 |
|------|--------|--------|----------|
| ① Prompt Chaining 链式 | 固定顺序多步,每步处理上一步输出,步间可加程序化"闸门"检查 | 任务能干净拆成固定子任务,用多次调用换准确率 | 写文案→翻译;大纲→检查→成文 |
| ② Routing 路由 | 先分类,再分发给专属处理 | 输入类别分明,分开处理更好 | 客服分流;简单题给小模型、难题给大模型 |
| ③ Parallelization 并行 | Sectioning:独立子任务并行 / Voting:同一任务多跑取多样 | 并行提速,或多视角提置信度 | 一个答题一个同时筛违规(护栏);多 prompt 审代码漏洞 |
| ④ Orchestrator-Workers | 中心 LLM **动态**拆任务、派工人、汇总 | 子任务无法预先定义的复杂工作 | 编程(改哪些文件取决于任务);多源搜集 |
| ⑤ Evaluator-Optimizer | 一个生成、一个评审,循环打磨 | 有明确评价标准、迭代确有提升 | 文学翻译;多轮搜索判断"查够没" |

与④的区别:①②③的子任务是预定义的,④是现场决定的。

## 6. Agents

- 前提:模型在理解、规划、用工具、错误恢复上足够成熟
- 形态:从指令/对话开始 → 任务明确后独立规划执行 → 可中途回来要信息
- **关键:每步从环境拿"真实反馈"(工具结果/代码执行)校准下一步**
- 可在检查点暂停等人;要有停止条件(如最大迭代数)防失控
- **实现往往很简单:就是循环里一个用工具的 LLM**(→ 本仓库 14 号)
- 适用:开放式、步骤无法预测、写不死固定路径的问题
- 代价:自主=高成本+错误复利 → 沙箱充分测试 + 护栏
- 例:编程 agent 解 SWE-bench;computer use

## 7. 组合与裁剪

模式是积木不是教条,自由组合变形;**度量表现→迭代**;
复杂度必须"被证明能改善结果"才加。

## 8. 三条核心原则

1. **Simplicity** 设计保持简单
2. **Transparency** 显式展示 agent 的规划步骤
3. **精心打造 ACI**(Agent-Computer Interface):工具文档和测试下大功夫

## 9. 附录 A:两个已验证场景

- **客服**:对话+工具天然结合(查订单/知识库/执行退款);成功可度量,
  有公司敢按"成功解决数"收费
- **编程**:产出可用测试自动验证,agent 拿测试结果迭代;人工 review 仍重要

## 10. 附录 B:给工具做提示词工程(最易被忽视)

- **工具定义值得和 prompt 同等的投入**
- 选模型"顺手"的格式:少转义、别让模型维护行号、贴近训练分布、给足思考 token
- **换位到模型视角**:用法一目了然吗?参数名/描述像给新人写文档字符串
- 大量测试模型怎么"用错"工具,迭代修
- **Poka-yoke 防呆设计**:把参数设计得让错误更难发生(如强制绝对路径)
- Anthropic 做 SWE-bench 时,优化工具的时间比优化总 prompt 还多

## 全文一图流

> **能简单绝不复杂**(prompt → +检索/示例 → workflow → agent)。
> 积木是 Augmented LLM;编排五式:链式/路由/并行/指挥官/评审循环;
> 真 agent = 循环里用工具的 LLM + 环境真实反馈 + 护栏,只配开放式问题。
> 三原则:简单、透明、把工具接口当产品做。
> 落地看客服和编程——因为成功可度量。框架慎用,先懂底层。

## 应用练习(本仓库作者的架构设计题)

**题**:客服自动回复,80% 固定问题(退货政策/运费),20% 复杂投诉,怎么设计?

**答**:入口加 **Routing**(一次便宜的分类调用);80% 走**纯 RAG 单次生成**
(检索→塞 prompt→回答,固定管道);20% 走 **Agent**(带查订单/退款工具 +
max_turns 护栏);出口加**人工兜底**(agent 搞不定/用户暴怒/涉及赔钱 → 转人工)。
整体 = workflow 骨架 + 局部 agent 心脏 + 业务护栏。
