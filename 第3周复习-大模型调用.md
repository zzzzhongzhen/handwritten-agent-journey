# 第 3 周复习:大模型调用(LLM API 核心)

> 本周从"会写 Python"跨到"会调大模型"。这些是做 Agent 的直接地基。
> 走 Kimi 路线(国内可用),用 openai 库 + base_url 指向 Kimi;概念可无缝迁移到任何大模型。

---

## 一、环境搭建

```bash
uv add openai          # Kimi 兼容 OpenAI 接口,用这个库
uv add python-dotenv   # 读 .env 里的 key
```
- key 存 `.env` 文件:`MOONSHOT_API_KEY=sk-xxx`(等号两边无空格、无引号)
- `.env` 必须加进 `.gitignore`(防止 key 泄露)
- 代码里 `load_dotenv()` 自动读 `.env`,不用手动 export

```python
from openai import OpenAI
client = OpenAI(
    api_key=os.environ.get("MOONSHOT_API_KEY"),
    base_url="https://api.moonshot.cn/v1",   # 换别家 API 主要就改这里 + key + 模型名
)
```

---

## 二、messages 结构(灵魂)

对话是一个**列表**,每条标明"谁说的"(role)+ 内容(content)。role 只有三种:

| role | 谁说的 | 用途 |
|------|--------|------|
| `system` | 你(开发者) | 设定身份/规则,开头一条 |
| `user` | 人类 | 你的提问 |
| `assistant` | 模型 | 模型的回复 |

```python
messages = [
    {"role": "system",    "content": "你是友好的助手"},
    {"role": "user",      "content": "我叫byron"},
    {"role": "assistant", "content": "你好byron"},
    {"role": "user",      "content": "我叫什么?"},
]
```
**role 是你手动填的**:人输入的填 user,模型回复的填 assistant。

---

## 三、发起一次调用

```python
response = client.chat.completions.create(
    model="kimi-k2.6",     # 用哪个模型(用 list_models.py 查账号可用的)
    messages=messages,     # 上下文:模型能看到的全部
    temperature=1,         # 随机性(kimi-k2.6 锁死=1)
)
reply = response.choices[0].message.content   # 取回复,记住这个路径
```

---

## 四、⭐ 无状态 + 多轮记忆(最核心)

> **大模型无状态:它不记得任何历史,每次调用都从零开始。**
> "对话记忆" = 你把每轮的 user 和 assistant 消息都存进 messages,每次把完整历史传回去。

多轮对话循环:
```python
while True:
    user_input = input("你: ")
    messages.append({"role": "user", "content": user_input})       # 存用户消息
    response = client.chat.completions.create(model=..., messages=messages)
    reply = response.choices[0].message.content
    print(reply)
    messages.append({"role": "assistant", "content": reply})       # ⭐ 存模型回复(关键!)
```

**关键区分:**
- `while True` = 让你能一直输入(交互外壳)
- `messages` 累加 = 让模型记得(记忆本体)
- 两者独立!网页版聊天没有 while,靠数据库存历史,一样多轮。

**验证技巧**:打印 messages 看历史里有没有 assistant;用 6 位随机数测(模型没法靠默认值蒙对)。

---

## 五、流式输出 streaming

```python
stream = client.chat.completions.create(..., stream=True)   # 开启流式
reply = ""
for chunk in stream:
    delta = chunk.choices[0].delta.content    # 注意是 delta 不是 message!
    if delta:                                 # 空块跳过
        print(delta, end="", flush=True)      # end="" 不换行,flush 立即显示
        reply += delta                        # 拼成完整回复
messages.append({"role": "assistant", "content": reply})   # 存的还是完整 reply
```

**要点:**
- 流式需要两个条件:`stream=True`(数据一块块来)+ `flush=True`&`end=""`(每块立即显示)
- `flush` 管"显示":print 默认把输出**攒在缓冲区**批量显示,flush 强制立即倒出
- "一段一段"出现是正常的:网络把多个块**打包传输**,不是代码问题
- 想平滑逐字:显示层加 `time.sleep(0.01)` 匀速吐字

---

## 六、大模型调用,黑盒里发生了什么

> 调用 = 把 messages + key + 参数通过网络发给远端服务器;模型读完全部 messages,
> **一个 token 一个 token 地预测/补全出接下来该说的话**,再发回给你。

- **模型跑在远端**,你只是"发文字、收文字"(和调天气 API 同形状)
- **本质是"预测下一个 token"**:把 messages 当已发生的对话,续写最合理的下文 → 所以 API 叫 completion(补全)
- **它只知道 messages 里的东西**:没记忆、不上网、不知道日期(除非你写进 messages)

这解释了一切:为什么无状态、为什么历史要自己传、为什么流式是一块块的。

---

## 七、token(处理单位)

- token = 模型处理文字的"最小块"(常用词一整块,生僻词拆碎片;中文约 1 字≈1~2 token)
- 模型是一个 token 一个 token 地生成的
- **三个实际影响:**
  1. **计费**:按 token 算(`usage.prompt_tokens` 输入 + `usage.completion_tokens` 输出),历史越长越贵
  2. **上下文窗口**:一次能看的 token 有上限,messages 超了会报错/被截断
  3. **记忆边界**:窗口外的看不到=记不住 → 聊久了要截断旧历史或做摘要

---

## 八、本周踩过的坑 / 排查经验

1. `export VAR = value` 等号带空格 → `bad assignment`(等号紧贴)
2. 模型名 404 → 用 `client.models.list()` 查账号真实可用的模型
3. `kimi-k2.6` 只允许 temperature=1 → 看报错信息按它说的改
4. API key 别进截图/聊天/代码,只进 .env
5. "失忆实验"要测模型自己说的话(不能测名字/42,会被 user 消息累加或默认值干扰)

---

## 九、你的成果

- `10_hello_kimi.py` — 第一次代码对话大模型
- `11_chat_loop_kimi.py` — 多轮记忆聊天
- `12_chat_stream_kimi.py` — 多轮 + 流式(= 第 1 个月里程碑完成版 🏆)
- `list_models.py` — 查可用模型

---

## 下一站:第 2 个月 · 手写 Agent Loop + 工具调用(分水岭)
本质:在 messages 里多塞一种东西(工具的执行结果),让模型"补全"时看到它、决定下一步。
地基还是本周这套"无状态 + messages + 预测下一个 token"。
