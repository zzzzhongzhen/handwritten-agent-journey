# 第 1 个月学习清单(Python + LLM API 跑通)

> 目标:能用 Python 写代码、能直接调通大模型、理解 LLM 的核心概念。
> 节奏:每周 8-12 小时。前两周补 Python,后两周打通 API。
> 你有 10 年工程底子,Python 对你是"补课"不是"重学",别焦虑,快速过。

---

## 第 1 周:Python 速成(只学你需要的)

你已经会编程,所以**跳过"什么是变量/循环"**,只看和 Swift 不同的地方。

- [ ] 安装环境:用 `uv`(现代 Python 包管理,比 pip 好用)
      - 装 uv: `curl -LsSf https://astral.sh/uv/install.sh | sh`
- [ ] Python 语法和 Swift 的差异(重点):
      - [ ] 缩进代替花括号、动态类型、`None` vs `nil`
      - [ ] list / dict / tuple(对应 Swift 的 Array / Dictionary)
      - [ ] f-string 字符串格式化:`f"hello {name}"`
      - [ ] 列表推导式 `[x*2 for x in items]`(很常用)
      - [ ] 函数、默认参数、`*args / **kwargs`
- [ ] `async / await`(你 iOS 用过,概念几乎一样,看语法即可)
- [ ] 类型注解 `def f(x: int) -> str:`(可选但建议用,你会喜欢的)
- **练习**:写个脚本,读一个文本文件,统计每个单词出现次数,打印 top 10

## 第 2 周:Python 工程化

- [ ] 虚拟环境 + 依赖管理(`uv init` / `uv add`)
- [ ] 读写文件、JSON 解析(`json` 模块)
- [ ] 发 HTTP 请求(`httpx` 或 `requests`)
- [ ] 环境变量管理(`os.environ`,`.env` 文件 + `python-dotenv`)—— API key 放这里
- [ ] 异常处理 `try/except`
- **练习**:写个脚本,调用任意一个免费公开 API(比如天气),解析 JSON 并打印

## 第 3 周:打通 LLM API ⭐

- [ ] 注册 Anthropic API key(console.anthropic.com),充几美元
- [ ] 安装 SDK:`uv add anthropic`
- [ ] 跑通本文件夹里的 `01_hello_llm.py`(最简单的单次对话)
- [ ] 理解这些概念(边跑边查):
      - [ ] messages 结构(role: user / assistant)
      - [ ] system prompt 是什么、怎么影响输出
      - [ ] token 是什么、上下文窗口、为什么有长度限制
      - [ ] temperature(随机性)
      - [ ] 流式输出 streaming
- **练习**:改 `01_hello_llm.py`,加上 system prompt,让模型扮演某个角色

## 第 4 周:多轮对话 + 第一个里程碑 🎯

- [ ] 跑通本文件夹里的 `02_chat_loop.py`(命令行多轮对话)
- [ ] 理解"对话历史"怎么维护(关键:LLM 无状态,历史靠你自己存着每次传回去)
- [ ] 加上流式输出,让回复一个字一个字蹦出来
- **🎯 里程碑**:一个能在终端里和你多轮聊天、记得上下文的命令行小工具
      - 做出来后,你就完成了 agent 学习的第一块基石

---

## 完成标准

第 1 个月结束时,你应该能:
1. 不查教程就能写基本的 Python 脚本
2. 解释清楚"为什么 LLM 是无状态的,对话历史要自己维护"
3. 有一个自己写的、能跑的命令行对话工具

做到这三点 → 进入第 2 个月(手写 Agent Loop,最关键的一个月)。

## 重要提醒
- **别报"包就业+分期贷"的班**。先用这个零成本路径验证自己能不能坚持。
- 卡住了随时回来问我,我帮你拆。
- 每天哪怕 30 分钟,**别断**。持续性 > 强度。
